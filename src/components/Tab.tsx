// "use client";

// import React, { ForwardedRef, forwardRef } from "react";

// import { cn } from "@/lib/utils";

// type Props = React.HTMLAttributes<HTMLDivElement>;

// export default forwardRef(function Tab(props: Props, ref: ForwardedRef<HTMLDivElement>) {
//   const { className, ...rest } = props;

//   return <div className={cn("w-full", className)} ref={ref} {...rest}></div>;
// });
